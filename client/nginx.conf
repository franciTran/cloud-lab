server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    location /api/ {
        proxy_pass http://mern-backend:5000/api/;
        proxy_set_header Host $host;
    }
}